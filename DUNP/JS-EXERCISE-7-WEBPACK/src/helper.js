export const printStudent = (s) => {
    document.write(`Student ${s.firstName} ${s.lastName} ima ocene ${student.subjects}`);
}

export const printSubjectsOfStudents = (st) => {
    document.write(`Prosek: ${st.calcAvg()}`);
}